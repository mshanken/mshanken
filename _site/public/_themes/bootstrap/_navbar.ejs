<!-- Start: Main Menu -->
<ul class="nav navbar-nav">
<% for(var menu in public._data){ %>
	<% if (menu !== "contact" && menu !== "privatepolicy") { %>
  		<li><a href="<%= public._data[menu].url %>" class="<%= menu %>"><%= public._data[menu].title %></a></li>
  	<% }; %>
<% }; %>
</ul>
<!-- End: Main Menu -->