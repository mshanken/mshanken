		
	<!-- <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarTogglerDemo01">
			<a class="navbar-brand" href="#">Hidden brand</a>
			<ul class="navbar-nav mr-auto mt-2 mt-lg-0">
		<%% for(var nav in public._data){ 
			if (nav !== 'sitemap') { %>
				<li class="nav-item <%%- current.source == nav ? 'active' : '' %>">
					<a href="/<%%= nav %>" class="nav-link"><%%= nav %><%%- current.source == nav ? '<span class="sr-only">(current)</span>' : '' %></a>
				</li>
			<%% }; %>
		<%% }; %>
			</ul>
		</div>
	</nav> -->

	<nav class="container-fluid bg-primary">
		<ul class="nav nav-pills nav-justified">
		<% for(var nav in public._data){ 
			if (nav !== 'sitemap') { %>
				<li class="nav-item">
					<a href="/<%= nav %>" class="nav-link<%- current.source == nav ? ' active' : '' %> text-white"><% if(nav === "index"){ %>home<% } else { %><%= nav %><% }; %></a>
				</li>
			<% }; %>
		<% }; %>
		</ul>
	</nav>